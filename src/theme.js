const root=document.documentElement;const saved=localStorage.getItem("ek-theme");if(saved==="light"){root.classList.add("light")}const btn=document.getElementById("themeToggle");function sync(){if(btn)btn.textContent=root.classList.contains("light")?"☾":"☀"}sync();btn?.addEventListener("click",()=>{root.classList.toggle("light");localStorage.setItem("ek-theme",root.classList.contains("light")?"light":"dark");sync()});
